<UserControl x:Class="FusionUI.Views.RuntimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="30"
                       FontWeight="SemiBold"
                       Foreground="#14324A"
                       Text="{Binding Title}" />
            <TextBlock Margin="0,14,0,0"
                       FontSize="14"
                       Foreground="#52606D"
                       TextWrapping="Wrap"
                       Text="{Binding Description}" />
        </StackPanel>

        <Grid Grid.Row="1"
              Margin="0,22,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Margin="0,0,10,0"
                    Padding="14"
                    Background="#E9F4F4"
                    CornerRadius="12">
                <StackPanel>
                    <TextBlock FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#2C6363"
                               Text="宿主摘要" />
                    <TextBlock Margin="0,10,0,0"
                               Foreground="#365164"
                               Text="{Binding Host.HostName}" />
                    <TextBlock Margin="0,6,0,0"
                               Foreground="#365164"
                               Text="{Binding Host.HostState}" />
                    <TextBlock Margin="0,6,0,0"
                               Foreground="#365164"
                               Text="{Binding Host.InitializationState}" />
                    <TextBlock Margin="0,6,0,0"
                               Foreground="#365164"
                               Text="{Binding Host.RuntimeInstanceId}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1"
                    Margin="10,0,0,0"
                    Padding="14"
                    Background="#F6F9FC"
                    CornerRadius="12">
                <StackPanel>
                    <TextBlock FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#274C77"
                               Text="运行环境" />
                    <TextBlock Margin="0,10,0,0"
                               Foreground="#365164"
                               Text="{Binding Host.Profile}" />
                    <TextBlock Margin="0,6,0,0"
                               Foreground="#365164"
                               Text="{Binding Host.RuntimeRoot}" />
                    <TextBlock Margin="0,12,0,0"
                               Foreground="#52606D"
                               Text="{Binding Hint}" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Margin="0,22,0,0"
                Padding="14"
                Background="#F8FAFC"
                BorderBrush="#D9E2EC"
                BorderThickness="1"
                CornerRadius="12">
            <StackPanel>
                <TextBlock FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#14324A"
                           Text="模块摘要" />
                <ItemsControl Margin="0,12,0,0"
                              ItemsSource="{Binding Modules}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,8"
                                    Padding="12"
                                    Background="White"
                                    CornerRadius="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock FontWeight="SemiBold"
                                                   Foreground="#1F2933"
                                                   Text="{Binding ModuleName}" />
                                        <TextBlock Margin="0,4,0,0"
                                                   FontSize="12"
                                                   Foreground="#52606D"
                                                   Text="{Binding ModuleId}" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="1"
                                               VerticalAlignment="Center"
                                               Foreground="#274C77"
                                               Text="{Binding State}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
