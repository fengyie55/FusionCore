<UserControl x:Class="FusionUI.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="30"
                       FontWeight="SemiBold"
                       Foreground="#14324A"
                       Text="{Binding Title}" />
            <TextBlock Margin="0,14,0,0"
                       FontSize="14"
                       Foreground="#52606D"
                       TextWrapping="Wrap"
                       Text="{Binding Description}" />
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,18,0,0"
                Padding="14"
                Background="#F4EDF8"
                CornerRadius="12">
            <TextBlock Foreground="#5B3F80"
                       Text="{Binding Projection.SummaryText}" />
        </Border>

        <Border Grid.Row="2"
                Margin="0,18,0,0"
                Padding="14"
                Background="#F8FAFC"
                BorderBrush="#D9E2EC"
                BorderThickness="1"
                CornerRadius="12">
            <ItemsControl ItemsSource="{Binding Entries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8"
                                Padding="12"
                                Background="White"
                                CornerRadius="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Foreground="#52606D"
                                           Text="{Binding Timestamp}" />
                                <TextBlock Grid.Column="1"
                                           Margin="12,0,0,0"
                                           Foreground="#274C77"
                                           Text="{Binding Level}" />
                                <TextBlock Grid.Column="2"
                                           Margin="12,0,0,0"
                                           Foreground="#7C3AED"
                                           Text="{Binding Category}" />
                                <StackPanel Grid.Column="3"
                                            Margin="12,0,0,0">
                                    <TextBlock FontWeight="SemiBold"
                                               Foreground="#1F2933"
                                               Text="{Binding Message}" />
                                    <TextBlock Margin="0,4,0,0"
                                               FontSize="12"
                                               Foreground="#52606D"
                                               Text="{Binding Source}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</UserControl>
