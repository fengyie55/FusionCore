<UserControl x:Class="FusionUI.Views.EquipmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBlock FontSize="30"
                       FontWeight="SemiBold"
                       Foreground="#14324A"
                       Text="{Binding Title}" />
            <TextBlock Margin="0,14,0,0"
                       FontSize="14"
                       Foreground="#52606D"
                       TextWrapping="Wrap"
                       Text="{Binding Description}" />
            <Border Margin="0,18,0,0"
                    Padding="14"
                    Background="#F8F4EA"
                    CornerRadius="12">
                <TextBlock Foreground="#6B5A2A"
                           Text="{Binding Hint}" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
