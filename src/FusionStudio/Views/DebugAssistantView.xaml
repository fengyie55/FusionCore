<UserControl x:Class="FusionStudio.Views.DebugAssistantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock FontSize="18"
                   FontWeight="SemiBold"
                   Text="{Binding Title}" />
        <TextBlock Margin="0,10,0,0"
                   FontSize="13"
                   Text="{Binding Description}" />
        <Border Margin="0,18,0,0"
                Padding="14"
                Background="#ECEAFD"
                CornerRadius="12">
            <TextBlock Foreground="#4C3AA9"
                       Text="{Binding Hint}" />
        </Border>
    </StackPanel>
</UserControl>
