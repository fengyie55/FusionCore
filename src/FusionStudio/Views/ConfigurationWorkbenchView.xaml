<UserControl x:Class="FusionStudio.Views.ConfigurationWorkbenchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock FontSize="18"
                   FontWeight="SemiBold"
                   Text="{Binding Title}" />
        <TextBlock Margin="0,10,0,0"
                   FontSize="13"
                   Text="{Binding Description}" />

        <Border Margin="0,18,0,0"
                Padding="14"
                Background="#FFF6E0"
                CornerRadius="12">
            <StackPanel>
                <TextBlock Foreground="#7C5E10"
                           Text="{Binding Hint}" />
                <TextBlock Margin="0,10,0,0"
                           Foreground="#7C5E10"
                           Text="{Binding Summary.Summary}" />
            </StackPanel>
        </Border>

        <Border Margin="0,16,0,0"
                Padding="16"
                Background="#F8FAFC"
                BorderBrush="#D9E2EC"
                BorderThickness="1"
                CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock FontWeight="SemiBold"
                           Foreground="#334E68"
                           Text="配置目录" />
                <TextBlock Grid.Column="1"
                           Foreground="#102A43"
                           Text="{Binding Summary.ConfigRoot}" />

                <TextBlock Grid.Row="1"
                           Margin="0,10,0,0"
                           FontWeight="SemiBold"
                           Foreground="#334E68"
                           Text="接线状态" />
                <TextBlock Grid.Row="1"
                           Grid.Column="1"
                           Margin="0,10,0,0"
                           Foreground="#102A43"
                           Text="{Binding Summary.IsConfigurationAvailable}" />

                <TextBlock Grid.Row="2"
                           Margin="0,10,0,0"
                           FontWeight="SemiBold"
                           Foreground="#334E68"
                           Text="模块覆盖" />
                <TextBlock Grid.Row="2"
                           Grid.Column="1"
                           Margin="0,10,0,0"
                           Foreground="#102A43"
                           Text="{Binding Overview.Modules.Count}" />
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
